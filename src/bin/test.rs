use glam::{vec2, vec3, Mat4};
// use
fn main() {
    // let fov_horizontal = (60f32).to_radians();
    // let size = vec2(8., 4.);
    // // let camera_screen_dist = (size.x * 0.5) / (fov_horizontal * 0.5).tan();
    // // let camera1_pos = vec3(0., 0., 0.);
    // // let camera2_pos = vec3(2., 4., 6.);
    // let point_pos = vec3(1., 2., 2.);

    // let projection_matrix =
    //     Mat4::perspective_infinite_lh(fov_horizontal, size.x / size.y, 0.001);
    // let res = projection_matrix.project_point3(point_pos);
    // pixel_space_to_3d_space
    // // let camera1_pixel_pos = vec2((camera1_point_transformed.x)/(camera1_point_transformed.z), y)
    // println!("{}", res)
}
